root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server.exe ./cmd/server"
  bin = "tmp/server.exe"
  delay = 1000
  stop_on_error = true
  include_ext = ["go"]
  exclude_dir = ["client", "node_modules", "tmp", ".git"]
